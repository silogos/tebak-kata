import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Home: NextPage = () => {
  return (
    <Layout title="MAIN MENU">
      <div>GAME</div>
    </Layout>
  );
};

export default Home;
